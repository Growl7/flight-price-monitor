# 杭州直飞河内机票价格监控系统 - 使用说明

## 系统概述

这个系统用于监控杭州直飞河内的机票价格，会自动抓取携程网站上的价格数据，并生成可视化报告。

## 功能特点

- 自动抓取携程网站上的机票价格
- 存储历史价格数据到SQLite数据库
- 生成静态HTML页面展示价格趋势
- 发送邮件通知每日最低价信息
- 支持定时任务自动运行

## 快速开始

### 1. 安装依赖

首先确保已安装Python 3.8+，然后运行：

```bash
pip install -r requirements.txt
```

### 2. 配置系统

编辑 `config.py` 文件，配置以下信息：

```python
# 邮箱配置（用于接收通知邮件）
EMAIL_CONFIG = {
    "sender": "your_email@qq.com",
    "receiver": "your_email@qq.com",
    "smtp_server": "smtp.qq.com",
    "smtp_port": 465,
    "password": "your_smtp_authorization_code"  # QQ邮箱授权码
}

# 航班配置
FLIGHT_CONFIG = {
    "departure": "HGH",  # 杭州萧山国际机场
    "arrival": "HAN",    # 河内内排国际机场
    "departure_city": "杭州",
    "arrival_city": "河内"
}

# 监控日期（2026年10月30日到2026年11月的每个周五）
MONITOR_DATES = [
    "2026-10-30",
    "2026-11-06",
    "2026-11-13",
    "2026-11-20",
    "2026-11-27"
]
```

### 3. 运行系统

```bash
python main.py
```

系统会显示菜单：

```
杭州直飞河内机票价格监控系统
==================================================
1. 启动定时监控 (每小时自动运行)
2. 运行单次抓取
3. 仅生成网页报告
4. 发送邮件通知
5. 退出
==================================================
请选择操作 (1-5):
```

## 详细使用说明

### 选项1：启动定时监控

选择此选项后，系统会：
- 立即运行一次抓取任务
- 每小时自动抓取航班价格
- 每天20:00发送每日汇总邮件
- 持续运行直到手动停止

**注意**：此模式需要保持程序运行，建议在服务器上部署。

### 选项2：运行单次抓取

系统会询问要抓取的日期：
- 输入具体日期（格式：YYYY-MM-DD）抓取指定日期
- 留空则抓取所有监控日期

### 选项3：仅生成网页报告

生成静态HTML页面和JSON数据文件，不进行抓取。

### 选项4：发送邮件通知

手动发送当前数据的邮件报告。

## 输出文件

### 数据库文件
- `data/flights.db` - SQLite数据库，存储所有航班价格数据

### 网页文件
- `output/flight_prices.html` - 静态HTML页面，可在浏览器中打开
- `output/flight_data.json` - JSON数据文件，可供其他程序使用

### 日志文件
- `data/app.log` - 系统运行日志

## 网页功能

生成的HTML页面包含：

1. **价格趋势图**：使用Chart.js绘制的每日最低价格折线图
2. **统计摘要**：监控天数、最低价格、平均价格、最高价格
3. **详细数据**：每个日期的所有航班信息表格

## 邮件通知内容

邮件包含：
- 监控摘要（日期范围、天数）
- 每日最低价格详情（价格、出现时间、航班号）
- 整体最低价统计

## 故障排除

### 1. 抓取失败

**可能原因**：
- 网络连接问题
- 携程网站反爬虫机制
- Chrome浏览器未安装

**解决方法**：
- 检查网络连接
- 稍后重试
- 安装Chrome浏览器

### 2. 邮件发送失败

**可能原因**：
- 邮箱配置错误
- SMTP授权码无效
- 网络连接问题

**解决方法**：
- 检查 `config.py` 中的邮箱配置
- 确认QQ邮箱已开启SMTP服务并获取授权码
- 检查网络连接

### 3. 网页生成失败

**可能原因**：
- 模板文件缺失
- 目录权限问题

**解决方法**：
- 确认 `templates/flight_template.html` 文件存在
- 检查目录写入权限

## 技术架构

```
机票价格监控/
├── config.py           # 配置文件
├── database.py         # 数据库操作模块
├── scraper.py          # 网页抓取模块（使用Selenium）
├── web_generator.py    # 网页生成模块
├── email_notifier.py   # 邮件通知模块
├── main.py             # 主程序
├── requirements.txt    # 依赖包列表
├── README.md           # 项目说明
├── 使用说明.md         # 使用说明
├── data/               # 数据目录
│   ├── flights.db      # SQLite数据库
│   └── app.log         # 日志文件
├── templates/          # HTML模板目录
│   └── flight_template.html
└── output/             # 输出目录
    ├── flight_prices.html  # 生成的HTML页面
    └── flight_data.json    # 生成的JSON数据
```

## 注意事项

1. **反爬虫机制**：携程网站有反爬虫机制，如果抓取失败，请稍后重试。

2. **运行时间**：定时任务模式下需要保持程序运行，建议在服务器上部署。

3. **浏览器要求**：系统需要Chrome浏览器才能正常工作。

4. **网络要求**：需要稳定的网络连接才能访问携程网站。

5. **邮箱配置**：QQ邮箱需要开启SMTP服务并获取授权码。

## 扩展功能

可以通过修改代码添加以下功能：
- 支持更多航空公司
- 添加价格提醒功能
- 支持多条航线监控
- 添加数据导出功能
- 集成其他通知方式（微信、Telegram等）

## 许可证

MIT License